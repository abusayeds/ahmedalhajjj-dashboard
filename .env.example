# Backend API base URL (no trailing slash)
# Local dev:
# VITE_API_BASE_URL=http://localhost:2002
# Cloudflare tunnel (always use https to avoid CORS redirect errors):
# VITE_API_BASE_URL=https://your-backend-tunnel.ssh.bd
VITE_API_BASE_URL=http://localhost:2002
